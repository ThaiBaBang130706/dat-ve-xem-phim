{
  "name": "cinema-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dashboard chỉ đọc cho server đặt vé Java TCP",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check public/dashboard.js"
  },
  "dependencies": {
    "chart.js": "4.5.1",
    "express": "4.22.2",
    "socket.io": "4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "4.8.3"
  }
}
